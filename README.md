# Art For Centuries — Static v2

Pure static HTML/CSS. Upload `index.html` and `README.md` to the root of a GitHub repo and deploy on Vercel.

Key edit points are inside `index.html`:
- `.brand` controls Art For Centuries typography
- `.kicker` controls the subtitle
- `.access-card` controls the invitation box
- `.sentence` controls the bottom statement
